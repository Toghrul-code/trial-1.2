#include <stdio.h>
int main() {
    int n, array[100];
    printf("Nece eded girilicek: ");
    scanf("%d", &n);

    for (int i = 0; i < n ; ++i) {
        printf("eded: ");
        scanf("%d", &array[i]);
    }
    for (int i = 0; i < n; ++i) {



    }
}